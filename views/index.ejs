<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <!-- Include bulma ui -->
    <link rel="stylesheet" href="css/bulma.min.css">
    <!-- Styling of card -->
    <style>
         .cell:hover {
            box-shadow: 0 25px 50px rgba(255, 221, 87, 0.2);
            border:1px solid hsl(48, 100%, 67%);
            transform: translateY(-10px); /* Move up 10px on hover */
        }
        
        .cell {
            border:1px solid #5a5959;
            transition: transform 0.3s ease, box-shadow 0.3s ease; /* Smooth transition */
            box-shadow: none;
  }
    </style>
</head>
<body>
    <div class="container">
        <p class="title  mt-5">NexChange</p>
        <p class="subtitle">Crypto Currency Exchange.</p>
        <div class="fixed-grid has-4-cols gap-5">
            <div class="grid">
                <% code.forEach(row => { %>
                        <div class="card cell">
                            <header class="card-header">
                            <p class="card-header-title"><%= row.name %> (<%= row.code %>)</p>
                            </header>
                            <div class="card-content">
                                <div class="content">
                                    <p>Amount : <%= row.minimal_amount %></p>
                                    <p>Withdrawal Fee : <%= row.withdrawal_fee %></p>
                                </div>
                            </div>
                            <footer class="card-footer">
                                <a href="#" class="card-footer-item">Learn more</a>
                            </footer>
                        </div>
                <% }) %> 
            </div>
        </div>
    </div>
</body>
</html>